{
  "name": "tunnel-fn-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter tunnel-fn-web dev",
    "build": "pnpm -r build",
    "build:lib": "pnpm --filter tunnel-fn build",
    "build:web": "pnpm --filter tunnel-fn-web build",
    "test": "pnpm --filter tunnel-fn test",
    "test:watch": "pnpm --filter tunnel-fn test:watch",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
